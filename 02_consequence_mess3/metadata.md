# 02_consequence_mess3 — designs A/B on Mess3
Question: does CONSEQUENCE (realized future depending on a circuit's output
through action+environment) make a pure next-token predictor build
self-structure? exp_A* = action-head design; exp_B* (+_rl) = self-resampling;
exp_inject*/exp_recall = seed injection/recall; efference.py, pre_post.py,
reset_aware.py, seed_to_belief*.py = follow-ups; circuit.py + CIRCUIT_FINDINGS.md
= whitebox decode of the A2 net. Uses core/ via symlinks; logs/ has exp* runs.
Run with cwd = this folder.
